--- dashboard/streamlit_app.py
+++ dashboard/streamlit_app.py
@@
-            f"Change values are bold below that threshold. WeakInitTimeLatest is the percentage "
-            f"gain from the price at InitTimeLatest to LastPrice and is bold below the C6 minimum "
-            f"gain ({float(SELL_CONFIG.get('c6_min_gain_percent', 2.0)):.2f}%)."
+            f"Change values are bold below that threshold. WeakInitTimeLatest is the percentage "
+            f"gain from the latest OPEN position's InitPrice to LastPrice. C6 is true only when "
+            f"C4 and C5 are false, the market is open, regular market close is within the next "
+            f"{float(SELL_CONFIG.get('c6_close_minutes', 30.0)):g} minutes, and WeakInitTimeLatest "
+            f"is below the C6 minimum gain ({float(SELL_CONFIG.get('c6_min_gain_percent', 2.0)):.2f}%). "
+            f"WeakInitTimeLatest is bold when C6 is true."
         )
